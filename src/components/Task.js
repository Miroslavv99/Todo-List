export class Task {
  constructor(taskTitle, taskDescription, taskDeadline, taskPriority) {
    this.taskTitle = taskTitle;
    this.taskDescription = taskDescription;
    this.taskDeadline = taskDeadline;
    this.taskPriority = taskPriority;
  }
}
